FROM apache/airflow:2.7.0-python3.10

USER root

# Copy DAG files
COPY dags/ /opt/airflow/dags/
RUN chown -R airflow: /opt/airflow/dags

USER airflow

# Install requests for HTTP calls to retraining service
RUN pip install requests
